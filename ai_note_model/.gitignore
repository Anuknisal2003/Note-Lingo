# ── Audio recordings (can be large, no need to commit) ──────────
dataset/audio/
dataset/prepared/

# ── Trained model files (very large, 300MB+) ────────────────────
model/

# ── Dataset splits (auto-generated by script) ───────────────────
dataset/train.json
dataset/test.json
dataset/vocab.json
dataset/metadata.csv